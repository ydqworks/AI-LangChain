---
description: Git 须人工下令才提交；commit 描述使用中文
alwaysApply: true
---

# Git 提交策略

## 禁止主动提交

- **不要**在完成修改后自动执行 `git add`、`git commit` 或 `git push`
- **不要**在任务结束时主动询问「是否需要提交」并自行发起提交
- **不要**将「帮你提交并推送」作为默认收尾步骤

## 仅在用户明确下令时提交

仅在用户**明确**要求时才执行提交相关操作，例如：

- 「提交」「commit」「帮我提交」
- 「推送到 GitHub」「git push」

若意图不清（如只说「保存一下」），先确认是否指 Git 提交，**不要**擅自 commit。

用户未要求 push 时，**只 commit，不 push**。

## Commit 描述规范（中文）

提交时 **commit message 必须使用中文**：

- **标题**：一句中文概括，50 字以内；可用 `类型：` 前缀（如 `文档：`、`功能：`、`修复：`、`配置：`）
- **正文**（可选）：中文说明「改了什么、为什么改」
- **不要**使用英文标题或中英混杂的默认描述

示例：

```
文档：完善 README 学习进度说明

补充路线图、环境状态与快速开始；新增禁止自动提交的 Cursor 规则。
```

```
功能：添加 LangSmith Tracing 示例

完成第 1 部分 1.3 步骤，可在 LangSmith UI 查看 trace。
```

## 允许的操作（无需用户下令）

- `git status`、`git diff`、`git log` 等只读查看
- 用户已要求提交时，按上述规范撰写中文 commit message

## 示例

```
用户：完善 README
→ 只改文件，不 commit

用户：提交
→ git add + commit，message 用中文；不 push

用户：推送到 GitHub
→ git push
```
